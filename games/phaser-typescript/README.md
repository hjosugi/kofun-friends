# Neon Kofun Dash — Phaser + TypeScript

屋上を30秒間走り、ドチキンさんをジャンプで避けるブラウザゲームです。

```bash
python3 games/sync_assets.py
cd games/phaser-typescript
npm install
npm run dev
```

Space、上矢印、W、クリックでジャンプします。配布用ファイルは
`npm run build`で`dist/`へ生成できます。
